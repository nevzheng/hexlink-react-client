/* istanbul ignore file */
/* tslint:disable */
/* eslint-disable */

import type { RedirectArray } from './RedirectArray';

export type User = {
    id: string;
    name: string;
    email: string;
    redirectsCreated: RedirectArray;
}
